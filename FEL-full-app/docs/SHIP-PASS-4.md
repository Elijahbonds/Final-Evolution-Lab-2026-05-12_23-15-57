# Ship Pass 4 — Stages, Movement, Menus, Wiring: the Full Picture (opened 2026-09-04)

Owner directive (verbatim): "lets do a pass for all of the stages being improved.
The movement systems being improved. Menus, wiring, full scope, full picture."

Owner decisions (2026-09-04, asked up front):
- **Venues:** BOTH — procedural props, crowds, lighting moods and skies on every
  baked map first; CC0 prop packs where a venue clearly needs them (each download
  named with source and size before it happens).
- **Bounds:** a NAVMESH generated offline from each venue's map mesh (a recast
  library; the exact package is named before it is added). The hand-typed clamps
  in ten modes retire.
- **Menus:** a CONSISTENCY pass — one shell for every mode (pause, results,
  controls card, phone join, venue preview), every mode reachable in three taps.
  No visual redesign.
- **Scope:** focused. The anatomy layer and the physics lesson stay with pass 3.

## The picture (measured 2026-09-04)

| Layer | State |
|---|---|
| Stages | 13 baked venues (2–3 MB, one texture each, no props, one lighting) + procedural skatepark / slope / surf worlds. `gridiron` and `sand-court` are referenced in `lib/map-data.ts` with no baked file. |
| Bounds | 10 modes clamp movement with typed constants (`ARENA_HALF`, `FIELD_HALF_X`…); `venue-bounds-tests` only checks the camera box. |
| Movement | 7 movement cores (court, board, combat, field run, carrier, ground ride, air) + `gameFeel`; nothing reads the body's mass; feel is hand-tuned per mode. |
| Camera | `CameraDirector` + `cameraPresets` per mode; 0 FEL-CAM fallbacks in the last sweep; occlusion/pull-in handled, no whiskers or asymmetric damping. |
| Menus | 85 routes; hub → `/modes` (one table, `MODE_INFO`) → `/play/<mode>`; 21 game components in the shell, several 500–800 lines with their own HUD and results. |
| Wiring | Phone bridge on 22 mode keys; sessions posted by the shell; 5 analytics events in total; `game_diag` to the admin endpoint; CI on push. |

## The ladder

| Phase | Deliverable | Gate |
|---|---|---|
| 1 Full picture | One matrix, mode × venue × movement core × camera preset × bounds source × phone bridge × session post × menu path × concept lock, generated by a script; every gap named. | Matrix complete; the gaps are the backlog. |
| 2 Stages | Every mode's venue upgraded on its baked map: props, crowd, lighting mood, sky; `gridiron` and `sand-court` baked; CC0 packs where needed. | Every venue re-shot; load budget held (≤3 s warm desktop). |
| 3 Bounds | Navmesh per venue from the map mesh; players and AI constrained by it; the ten clamps retire. | No out-of-bounds across a 21-mode sweep; clamp constants gone. |
| 4 Mass model | de Leva segment masses on the rig feed acceleration, turning inertia, jump impulse and landing load through the movement cores. | Numbers measured per body; gauntlet green. |
| 5 Movement feel | Per-sport acceleration, turn radius, stop distance from the mass model, benchmarked (2K, Skate 3, SSX, Madden feel targets); `gameFeel` on every mode. | Owner feel review; gauntlet green. |
| 6 Camera | Whiskers, occlusion fade and pull-in, asymmetric damping, per-mode context on every preset. | FEL-CAM fallbacks 0 across a sweep; owner review of three modes. |
| 7 Menus | One shell: pause, results, controls card, phone join, venue preview; hub → mode ≤ 3 taps; Modes screen shows venue + controls. | Every mode reachable in three taps; results screen on every mode; phone captures clean. |
| 8 Wiring | Phone bridge on every playable mode; a session and PRQ deltas from every mode; funnel analytics per mode; diag on every mode. | Matrix all green; events land on the admin endpoint. |
| 9 Performance | New bodies + new venues on the mobile tier and phones: VRAM and fps budgets, texture tiers, load budget. | Mobile-tier seven ≥ 30 fps under 4× throttle; phone seven clean. |
| 10 RC | Production sweep, docs, changelog, tag. | Tagged candidate; sign-off signed. |

Sweeps run at phase boundaries; the `/loop` gauntlet reports regressions only.

## Findings log
- **4 Sep, phase 1 done.** `scripts/full-picture.mts` writes `SHIP-PASS-4-MATRIX.md`: 24 registry modes × venue spec, kit builder, baked map, movement cores, camera preset, typed clamps, phone bridge, gauntlet, play sweep, concept lock, test scripts, file size. 40 gaps. The shape of them: only 3 of 18 enabled modes stand on a baked map (karate, tennis, 3v3) although 13 maps are baked — dunk, ones, carnival and dunk duel build a kit court next to an unused Venice court; derby, penalty and golf build a kit field beside baked park, stadium and links; skate, snowboard and surf run procedural worlds beside baked park, slope and break. Seven enabled modes have no camera preset (penalty, golf, carnival, mixedcombat, dunkduel, dance, threepoint); 9 carry typed clamps (36 in all); 4 enabled modes lack a shared movement core (football, surf, onevone, mixedcombat use mode-local movement). The matrix cannot see venues mounted by a shared base (the board modes delegate to BoardRunMode), so it under-reports the board modes' kit builders.
- **4 Sep, phase 2 opened.** The raw `gridiron.glb` and `sand-court.glb` carry no textures (the pipeline refuses them as grey soup), so football and volleyball keep procedural venues and get their props in the procedural pass. First wiring: the four basketball venue specs (dunk, ones, 3v3, carnival) all point at the scanned Venice court, whose placement was tuned so the painted baseline hoop sits under the rim at world origin — the same rim position every basketball mode uses.
- **4 Sep, the naked hero, twice explained.** After the swap the hero and every rival played bare-legged in the modes while the Closet dressed them. Two causes, both measured. (1) MakeHuman garment maps carry a low alpha (jean shorts 0.49, boots 0.21): Blender exported them BLEND, which rendered as near-transparent cloth, and a half cutoff discarded them — garments are OPAQUE now. (2) The anime ink outline is an inverted hull drawn with depth, and the body's hull buried anything tighter than the ink width — thin widths still speckled, a positive depth offset made it worse, a negative one (−12 / −48 units) pushes every hull behind the surfaces it wraps: cloth wins, the contour stays at the silhouette. Four more modes sit on their venue specs (karate VS, showdown, three-point, dunk duel); captures clean.
- **4 Sep, phase 2 approach fixed.** Two venue paths exist: a venue SPEC (ground with painted markings, props, crowd tiers, lighting, optional baked map) and the procedural VenueKit builders. The spec is the richer one and the maps hang off it, so the phase moves every mode whose gameplay geometry is its own (rims, goals, holes, mounds built by the mode) onto a spec sized to that geometry, kit as fallback. Done: dunk, ones, 3v3, carnival, karate VS, showdown, three-point, dunk duel. Staged: golf (60×90 green, holes at z 26–39), derby (70×90 diamond), penalty (50×70 pitch, goal line z 10.4), football rush (44×52). Kept procedural: skate, snowboard, surf, gymnastics, big air (their worlds ARE the gameplay) and mixed combat (its ring top is the floor) — these get scenery, sky and lighting moods in the procedural pass instead of a spec.
- **4 Sep, owner decisions on the two specifics.** (1) Bounds: `recast-navigation@0.43.1` (MIT; 330 KB JS + 2.0 MB WASM) enters as a DEV dependency only — each venue's navmesh is generated offline by a script and shipped as small polygon data, and the game constrains movement with its own polygon test; no WASM in the browser. (2) Props: six Kenney CC0 kits from the shorepine/kenney mirror — Nature (3.0 MB, 329 models), Mini Skate and Mini Arena (1.1 MB, 42), City Kit Suburban (2.6 MB, 40), Racing and Minigolf (3.8 MB, 238) — fetched by sparse clone into `~/Developer/FEL-swarm/tools/kenney`; only the props a venue uses are copied into the app, trimmed.

### 2026-09-04 — phase 2, venues: specs for the four field modes, CC0 prop dressing everywhere
- **Specs**: `golf_loop` (60×90 green), `derby` (70×90 diamond), `penalty` (50×70 pitch, goal line z 10.4) and `football_rush` (44×52 gridiron) joined VENUE_SPECS with map keys coastal-links / baseball-park / soccer-stadium. Golf, Baseball, Soccer and Football Rush now mount spec-first with the kit as fallback (same pattern as karate/dunk/ones). 12 of 16 mode venues are on specs.
- **Props**: `scripts/venue/curate-props.mts` copies 68 models (1.1 MB with the one texture atlas) from the six owner-approved Kenney kits into `public/models/props/<kit>/` with `manifest.json` recording CC0. `visual/VenueProps.ts` loads each model once per scene and instances it; `visual/venuePropSets.ts` holds ten placement tables (venice-court, dojo, links, ballpark, stadium, gridiron, skatepark, slope, surf-break, gym). Spec venues get theirs from `mountVenue` (NexusVenue, not the fenced NexusWebScene); the board runs and air sessions call the loader directly. Unlit kit materials are switched to lit PBR so they sit in the IBL.
- **Gate**: `venuePropSets.test.ts` pins every model file present and every placement outside its play area (the gym gantry failed it at the origin — moved behind the start).
- **Corrections after the first frames**: (1) the three field maps are ±15 m walkabout islands — derby and penalty play inside theirs and look right; the golf course runs to z 39, so `golf_loop` keeps the procedural green (map key removed) under a links daylight sky. (2) Football keeps the kit gridiron (yard lines, posts) under the spec's sky and lights — the spec ground hides, its goal prop went. (3) The scanned Venice court carries its own fence and crowd, so the court set is palms and lamps outside it. (4) Kenney nature models are ~1.5 m at unit scale: trees now 4.4–5.2×, bushes 3.4×. (5) Four more dead twins found the hard way: `BoardRunMode.ts` as well as the golf/baseball/soccer files; the registry serves `SkateRunMode` / `SnowboardSlalomMode` / `SurfBreakMode` and `precisionModes`.
- **Gap logged (pre-existing)**: snowboard slalom captures as a whiteout — identical in the 2026-09-03 sweep frame, so not from this change; the piste fog/exposure needs its own look in the movement/venue pass.
- **Sweep note**: the 10:43 gauntlet ran while penalty and football were being wired (errors 2/6 from hot reload); both capture clean on the settled code. Not promoted as the baseline.
- **Golf, final shape**: the spec's own ground material rendered a pale mint under the venue grade whatever the sky was set to (three environments tried), while football's kit field under the same spec pipeline stayed saturated — so golf, like football, keeps its kit field (green, pines) and hides the spec ground; the spec supplies sky, props and camera bounds. **Follow-up**: Kenney vertex-coloured trees read teal under the spec lighting but green in kit venues (skatepark) — check the hemispheric/IBL colours against the vertex colours before phase 9.
- **Open**: `public/models/maps/baked/*.glb` (all twelve, +40 % each) and its manifest (emptied to `{}`) were rewritten at 10:18 and 11:22 today — both moments coincide with a vitest run, yet no test, suite script or package hook references the map pipeline, and the folder is neither a symlink nor filtered. Restored from HEAD both times; the pipeline now merges its manifest; the gauntlet prints a WARNING row if the folder drifts during a sweep.

### 2026-09-04 — phase 3, bounds: navmeshes baked from the map meshes
- `recast-navigation@0.43.1` installed as a dev dependency (owner decision). `scripts/venue/navmesh-gen.mts` applies the mounter's exact transform (glTF x-mirror, mapRotationY, scale, mapOffset, surfaceY drop) and bakes ten of twelve maps to `public/models/navmesh/<key>.json` (52 KB total; coastal-links and venice-blacktop fail Recast's heightfield — neither is under a mode's play right now). `core/NavBounds.ts` is the runtime: grid-indexed convex polygons, `contains` and `constrain` (nearest edge), no dependency shipped; three tests.
- **Measured before wiring** (`scripts/probes/_navmesh-holes.mts`, 0.5 m samples over each mode's play rectangle): the raw mesh left 2.7 % of the half court uncovered — a hole under the rim assembly — and 21.6 % of the dojo mat, pinched to ±4 m mid-mat by hanging geometry at head height; soccer 27 %, gym 33 %. A sport can't lose its key or its mat to a lantern, so each map gets a **regulation play core** (from the specs' grounds) merged into the bake; the mesh contributes the walkable area beyond it (sidelines, steps, apron). After the cores every gameplay rectangle samples 0 % uncovered (skatepark stays 87 % — bowls, and that mode plays on its kit).
- Next: `mountVenue` loads the nav for the spec's map key; 1v1 and 3v3 (`clampToHalfCourt`), karate VS and showdown (`ARENA_HALF`) constrain through it when present, box clamp otherwise; a sweep row per mode for out-of-bounds.
- **Wired**: `mountVenue` loads the navmesh for the spec's map key and the handle exposes `nav` and `constrain(pos)`; 1v1 and 3v3 route `clampToHalfCourt` through it, karate VS and showdown route `ARENA_HALF` through it (showdown's box was 24 m on a 14 m dojo floor — the mesh is the first real bound it has had). Box clamps stay as the fallback for kit venues and the headless suites, which mount no venue. Probe: `[NEXUS] navmesh "venice-blue-court": 38 polys` on /dev/mode/onevone.
- **Gauntlet**: `scripts/gauntlet.sh` word-split was zsh-only (`${=r}`), which dropped the seven mobile rows when run under bash; now `$(printf …)`, which splits in both shells. The 12:00 sweep after the phase 2 commit was clean: 250 tests, every mode row 0/0/0, no baked-map drift.
- **Dojo core corrected**: the first showdown frame on the mesh put the rival half inside the side wall — the ±4 m pinch in the raw dojo mesh is the map's pillars, not lanterns, so a 14 m core overrode real walls. The dojo core is now the clear mat between the pillars (±4.5 m, karate VS's own `ARENA_HALF`); the mesh supplies the wider ends. Lesson for every core: size it to the clear floor the map actually has, then let the mesh add — never the other way round.

### 2026-09-04 — phase 6, camera: the framing survey
- The matrix's camera column read `lib/babylon/config/cameraPresets.json`, a table measured 2026-07-28 on the Nexus **venue** cameras (`nexus_cam_*`, target fraction 0.22). Since M104 every gameplay mode keeps its own follow camera and the venue camera is scenery, so that table describes cameras nobody sees; the six "missing presets" were a stale artefact. Every mode already has a follow preset (`camPreset`).
- **Measured instead** (`scripts/probes/_cam-frame.mts`, hero height ÷ viewport height, six samples per mode, 1280×800), recorded in `lib/babylon/config/cameraFraming.json`: dunk 0.37, ones 0.33, dunk duel 0.26, three-point 0.66, 3v3 0.14, karate VS 0.47, karate endless 0.70, showdown 0.68, mixed combat 0.44, skate 0.33, surf 0.35, slalom 0.33, big air 0.27, gym 0.27, volleyball 0.06, tennis 0.055, golf 0.048, derby 0.47, penalty 0.34, football 0.26, carnival 0.26, dance 0.25. Median across modes 0.33; spread 0.05–0.70.
- **Reading**: the three fixed wide cameras (volleyball, tennis, golf) put the hero at 40 px tall on an 800 px frame — below any of the sports' benchmark framings; three-point, karate endless and showdown are at the close end (two thirds of the frame); 3v3 is the far outlier among follow cams. Phase 6 tunes those seven toward the band the rest sit in, re-measures, and moves the matrix column to this file.
- **Regression (sweep 12:28, after the phase 3 commit)**: karate VS `FEL-FRAME 6` (was 0). Cause: the navmesh let the fighters into the dojo's end alcoves past the 4.5 m arena, where the pillars box the fight camera in. Fix staged: karate VS constrains to the floor (navmesh) AND the arena box — the arena is a design choice smaller than the floor. Showdown keeps the mesh alone (its old 24 m box was the defect there). Every other row 0/0/0; 253 tests; no baked-map drift; mobile rows back (7).
- **Found through the survey**: tennis and volleyball were rendering through the venue's orbit camera (`nexus_cam_tennis` / `nexus_cam_volleyball`) — NetSportMode and DanceMode mounted their venues without `keepGameplayCamera`, the M104 gap left open. In tennis the hero sat cut off at the bottom edge at 44 px while the opponent was what the frame held. With the flag: tennis 0.36, volleyball 0.60, dance 0.69 on their own follow cameras, FEL-FRAME 0. Golf's 0.048 is the hole-reveal cinematic at the sample moment; re-measure at address before tuning.
- **Landed**: follow lag normalised to frame time and biased (catch-up ×1.6, settle ×0.8) so a 30 fps phone no longer lags twice as far as a desktop; ±18° whiskers lean the shot 8° toward the open side before the hard ±50° swing is needed; `window.__FEL_DEV__.hero` for probes; four playable routes (volleyball, showdown, duel, sprint) added to MODE_INFO so the Modes screen lists them; matrix camera column now reads `cameraFraming.json` (gaps 35 → 29 → recount after this commit).

### 2026-09-04 — phase 7, menus: the consistency survey
- **Measured**: 30 play routes; 26 render through `GameShell` (title, venue, one control deck); music, acting and IRL are studio experiences with their own full-screen UI, not sports modes — left as they are. All 16 Babylon game components report a result to the shell (`onEnd`), so a results screen exists on every mode. The harness pauses every mode on START, but `BootSplash` renders nothing while paused, and 13 of 16 components draw their own "PAUSED — TAP TO RESUME"; air session, sprint and three-point freeze with no overlay — patch staged. The Modes screen lists `MODE_INFO` with name and venue; volleyball, showdown, duel and sprint were missing from it and are in as of the phase 6 commit.
- **Owner asks pending** (not started, per the standing rule on physics): phase 4 mass model and phase 5 movement feel change acceleration, turning inertia, jump impulse and landing load through the movement cores — they wait for sign-off. Phases 6 and 7 continue meanwhile.
- **Close framing, measured to cause** (probe now reports camera-to-hero distance): three-point 4.04 m against a preset asking ≥ 6.2 — the M64 camera box is the 16 m half-court ground, and the corner racks want the camera at x ≈ ±11, so the clamp pins it; showdown 3.98 m ≈ its fight preset (4.2), close by design, as is karate endless on the over-shoulder preset. Fix staged for three-point: the camera box becomes the ground box ∪ the map's measured walkabout bounds (±13 for the Venice court), with occlusion still handling the fence.
- **Three-point fixed by measurement**: the camera box is now the spec ground ∪ the map's measured walkabout bounds (`NexusVenue.mountVenue`); three-point measures 0.35 at 6.74 m (was 0.66 at 4.04 m), FEL-FRAME 0. The 13:00 sweep after the phase 6 commit: clean, karate VS back to 0, 253 tests, seven mobile rows, no map drift.
- **Pause overlay**: air session, sprint and three-point now draw "PAUSED — TAP TO RESUME" like the other thirteen. Verified through `scripts/probes/_pause-overlay.mts` (logs in as the playtest user, opens /play/<route>, `j` past the READY gate, Escape = START to pause, `j` to resume) — the dev harness renders no game component, so the first probe attempt on /dev/mode proved nothing.
- **Matrix recount** (script fixed to judge each mode by its own slice of a shared file, and to mark navmesh-first clamps): 26 gaps. Derby and penalty read as mapped now (the script had taken golf's mount for all three precision modes). Every enabled mode already shows ✓ for phone bridge, gauntlet, play sweep, concept lock and tests — phase 8's matrix gate is met on paper; what remains is proving sessions and diagnostics land on their endpoints from a real run.

### 2026-09-04 — phase 8, wiring: proved from a real run
- `scripts/probes/_session-e2e.mts` logs in as the playtest user, opens `/play/<route>`, passes the READY gate and lets the game run to its natural end while watching the network. Karate versus: idle player defeated 0–2 in 78 s → `POST /api/sessions` 200 (`xp 10, shards 1, credits 5, prqDelta 0`), results card with the rewards, one `GameSession` row (`karateVersus`) and `session_complete` in `AnalyticsEvent` — checked in the database with `scripts/probes/_db-today.mts`. No `game_diag` events fired, which matches the clean run. The same probe now runs across the other twenty playable routes.

### 2026-09-04 — phase 9, performance: texture memory measured per mode
- `scripts/probes/_vram-diag.mts` sums every scene texture (w×h×4, ×1.33 mipmapped; the dev HUD's "vram ~" number) and lists the largest. Desktop: dunk 199 MB, threes 71 MB, karate VS 75 MB. Mobile tier (phone-shaped context): dunk 168 MB, threes 40 MB — the tier drops the 2048² cascaded shadow map (16 MB) as designed, but dunk keeps three 2048² skin maps at 21 MB each (the hero's baked skin base colour, the `light-male` family map and the detail normal) because dunk is the mode that applies a full identity on the harness; every logged-in player takes that path in every mode. Next: 1024² skin-map variants chosen by tier, and a check whether the hero GLB's own 2048² skin can carry a tier variant.
- **1024² skin set written** (`export-skins.mts --size 1024 --suffix -1024`: six family maps + the detail normal, 443 KB beside the 2048 set; manifest and skinLibrary untouched). Next: `applySkinMap` reads `scene.metadata.felTier` and takes the `-1024` files on mobile — saves ~48 MB of the 64 MB on a phone; the hero GLB's own 2048² skin (21 MB) needs a tier variant of the body file, a separate decision.
- **Twenty-route session run** (`_session-e2e.mts`, passive play, 300 s cap): posted with rewards — onevone 54 s, threevthree 94 s, mixedcombat 56 s, skateboard 94 s, surf 94 s, big-air 18 s (xp 460), volleyball 232 s, tennis 62 s, baseball 26 s, dance 56 s (xp 168), plus karate-vs 78 s earlier: 12 of 20. Never reached an end under passive play: dunk, threepoint (controller-link room + analytics posted, no session), karate endless, snowboard, gymnastics, golf, soccer, football — every one a contest or run that needs the player's input to finish, so the pipe is unproven there rather than broken. dunkduel, showdown and carnival produced no row (no canvas; likely the disabled-route redirect to /modes) — to confirm. Next: an active-input pass (j/k/l pumped every 1.5 s, 420 s cap) on the eight, after the perf lane's throttle measurements finish, since a second browser on this host would depress them.
