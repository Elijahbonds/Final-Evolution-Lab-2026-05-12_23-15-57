# Release candidate — 2026-09-04 (ship passes 3 and 4, phase 10)

RC lane of the orchestrated pass 4 run (`docs/CONTRACTS-PASS4-RUN.md`). Worktree `lane-rc`, branch `lane/rc`, base
`476cfc2`. Every number here was measured on this machine on 2026-09-04 between 15:01 and 15:22; nothing is copied
from an earlier doc. The tag is the orchestrator's at integration — none is created here.

## 1. Production build

`zsh scripts/prod-serve.sh build` (`NEXT_DIST_DIR=.next-verify next build`, Next.js 14.2.35, `.env.local` loaded).

| Measure | Value |
|---|---|
| Exit code | 0 |
| Wall time | 62.8 s (`/usr/bin/time -p`: real 62.82, user 99.19, sys 10.47) |
| Type check | "Checking validity of types" passed (`ignoreBuildErrors: false`) |
| Static pages | 80/80 generated |
| Warnings | 1 — `@mediapipe/tasks-vision/vision_bundle.mjs`: "Critical dependency: the request of a dependency is an expression", imported through `components/facescan/face-scan-capture.tsx` → `components/closet-view.tsx`. Pre-existing (the same import shipped with 0.9.0-rc.1); webpack cannot statically resolve MediaPipe's loader expression. |
| Experiments flagged | `outputFileTracingRoot` (set in `next.config.js`) |

Route table summary (every route is `ƒ` dynamic, server-rendered on demand; middleware 26.5 kB):

| Group | Routes | First Load JS (notable) |
|---|---|---|
| Total | 209 | shared by all 89.8 kB |
| `/api/*` | 127 | 0 B (server only) |
| `/play/*` | 35 | Babylon game routes 155–156 kB; `/play/carnival` 186 kB; `/play/mirror` 2.02 MB; `/play/map-preview` 361 kB |
| `/dev/*` | 7 | `/dev/mode/[key]` 2.08 MB, `/dev/rig` 1.85 MB, `/dev/anim` 361 kB — built, but every `/dev/*` page hard-404s when `NODE_ENV !== 'development'` (verified in §2) |
| Other pages | 40 | `/` 183 kB, `/closet` 2.02 MB, `/camp` 184 kB, `/studio` 188 kB, `/profile` 181 kB |

Full log: `/private/tmp/claude-501/lane-rc/build.log` (session scratch, not committed).

## 2. Production server on :3006

`scripts/prod-serve.sh` now takes a port (`start|stop|status [port]`, or `PORT=`); the log and pid file follow the port.
`zsh scripts/prod-serve.sh start 3006` answered on the first poll.

| Check | Result |
|---|---|
| `GET /` | 200 |
| `GET /dev/mode/dunk`, `/dev/anim`, `/dev/rig` | 404, 404, 404 |
| Security headers on `/` | `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `X-Frame-Options: SAMEORIGIN`, `Content-Security-Policy: frame-ancestors 'self'`, `Permissions-Policy: camera=(self), microphone=(), geolocation=()`; no `X-Powered-By` |
| `GET /play/dunk` anonymous | 307 → `/login` |

## 3. Production sweep

**Dev-mode harness routes return 404 in production (by design); the production sweep is the play-route set below.**
`scripts/gauntlet.sh` was started as specified (`BASE=http://localhost:3006 GAUNTLET_DIR=… bash scripts/gauntlet.sh`); its
first two rows are real (`tsc : PASS`, `vitest : Tests 253 passed (253)`), and every `/dev/mode/<key>` row then fails the
same way — verbatim from `logs/dunk.txt`:

```
dunk route : /dev/mode/dunk
page.waitForSelector: Timeout 30000ms exceeded.
Call log:
  - waiting for locator('canvas') to be visible
```

— about 60 s per row (8 s login-form wait, 18 s canvas loop, 30 s selector timeout). Four rows (`dunk`, `threepoint`,
`threevthree`, `onevone`: `NO RESULT`) were recorded before the orchestrator stopped the run; the file is kept as
`harness-aborted-run-20260904-150509.txt` in the sweep dir. The 21 harness rows, the 3 `login/*` rows and the 7 `mtier/*`
rows all target `/dev/mode/*` and cannot be measured against a production server; the shipping routes below are the
sweep that can.

Environment: the other lanes' Chrome captures were running against :3000 and :3005 on this machine throughout (seen in
`lsof` before the sweep). Frame-guard, missing-clip, error and identity results do not depend on that; load times might.

### 3a. Shipping routes, logged in (`capture-mode-play.mts`, `LOGIN=1 … REPS=6`, 1280×800), 15:11–15:18

Run file `play-20260904-151124.txt`; per-row logs `logs/prod-<route>.txt`. `perf` reads `n/a` on every row because
the fps/draw readout is the dev harness HUD, which the shipping page does not render. Every line is as measured:

```
prod/dunk         : FEL-FRAME 0 | MISSING CLIP 0 | errors 0        /play/dunk         n/a  | 1.4 s (loaded) · ttp 15.1 s (playing) | [FEL-IDENT] ready: 6 visible meshes
prod/karate       : FEL-FRAME 0 | MISSING CLIP 0 | errors 0        /play/karate       n/a  | 2.0 s (loaded) · ttp 13.1 s (playing) | [FEL-IDENT] ready: 6 visible meshes
prod/skateboard   : FEL-FRAME 0 | MISSING CLIP 0 | errors 0        /play/skateboard   n/a  | 1.1 s (loaded) · ttp 13.1 s (playing) | [FEL-IDENT] ready: 6 visible meshes | [FEL-IDENT] ready: 6 visible meshes
prod/volleyball   : FEL-FRAME 0 | MISSING CLIP 0 | errors 0        /play/volleyball   n/a  | 1.5 s (loaded) · ttp 13.1 s (playing) | [FEL-IDENT] ready: 6 visible meshes
prod/golf         : FEL-FRAME 0 | MISSING CLIP 0 | errors 0        /play/golf         n/a  | 1.2 s (loaded) · ttp 13.1 s (playing) | [FEL-IDENT] ready: 6 visible meshes
prod/football     : FEL-FRAME 0 | MISSING CLIP 0 | errors 0        /play/football     n/a  | 1.1 s (loaded) · ttp 13.1 s (playing) | [FEL-IDENT] ready: 6 visible meshes
prod/dance        : FEL-FRAME 0 | MISSING CLIP 0 | errors 0        /play/dance        n/a  | 1.2 s (loaded) · ttp 13.1 s (playing) | [FEL-IDENT] ready: 6 visible meshes
prod/tennis       : FEL-FRAME 0 | MISSING CLIP 0 | errors 0        /play/tennis       n/a  | 1.2 s (loaded) · ttp 13.1 s (playing) | [FEL-IDENT] ready: 6 visible meshes
prod/onevone      : FEL-FRAME 0 | MISSING CLIP 0 | errors 0        /play/onevone      n/a  | 1.2 s (loaded) · ttp 13.1 s (playing) | [FEL-IDENT] ready: 6 visible meshes
prod/threevthree  : FEL-FRAME 0 | MISSING CLIP 0 | errors 0        /play/threevthree  n/a  | 1.5 s (loaded) · ttp 13.0 s (playing) | [FEL-IDENT] ready: 6 visible meshes
```

10 of 10 rows 0/0/0; identity ran on every row (`FEL-IDENT ready`, 6 visible meshes — the kit hero's body, garments and
hair); time-to-loaded 1.1–2.0 s, all under the 3 s warm budget; time-to-playing 13.0–15.1 s is the script's own start
delay (measured as such on 2026-09-03), not the load. Skateboard printed two `FEL-IDENT ready` lines — recorded, not
explained here.

### 3b. Phone touch rows (`capture-mobile-touch.mts`, the gauntlet's last block, same verbs), 15:18–15:19

```
mobile/skateboard : errors: 0
mobile/volleyball : errors: 0
mobile/tennis     : errors: 0
mobile/dunk       : errors: 0
mobile/karate     : errors: 0
mobile/football   : errors: 0
mobile/golf       : errors: 0
```

7 of 7 rows `errors: 0`; each log shows the touch targets resolved and the mode's HUD after the touches (e.g. dunk:
`FLIGHT NIGHT | Venice Beach Court | … | CHARGE | X | STYLE | SLAM`; golf: `THE LOOP | Coastal Links | … | CLUB | SWING`).
No baked-map drift after the sweep (`git status --short public/models/maps/baked` empty).

**Not measured against production:** the mobile quality tier (`TIER=mobile` rows) — the gauntlet runs them on
`/dev/mode/*` only, and the perf lane owns the tier work this run; and fps on the shipping routes (no HUD there).

## 4. RC checklist (`scripts/rc-checklist.mts`)

`GAUNTLET_DIR=/private/tmp/claude-501/lane-rc/gauntlet npx tsx scripts/rc-checklist.mts`, run on the working tree
before the lane's commits (hence the `git HEAD` row). Verbatim:

```
RC checklist — 2026-09-04 15:22 (local) — /Users/elijahbonds/Developer/FEL-swarm/copilot-worktrees/final-evolution-lab-2026-05-12_23-15-57/lane-rc/FEL-full-app
-----------------------------------------------
PASS  tsc --noEmit                         exit 0; 0 error line(s) outside .next*/types
PASS  vitest run                           exit 0; Test Files 39 passed (39); Tests 253 passed (253)
FAIL  texture budget (mobile ≤ 2× median)  measuredAt null — the perf lane's probe has not written the table (mobile 0 mode(s))
info  gauntlet (latest run-*.txt)          no run file
PASS  play gauntlet (latest play-*.txt)    play-20260904-151124.txt: 17 row(s), 0 non-clean
FAIL  git HEAD                             476cfc2 (lane/rc), 2 tracked file(s) modified
info  rc tag at HEAD                       none (the orchestrator tags at integration)
-----------------------------------------------
2 gate(s) failing; 75 s
```

Reading: the two failing gates are expected at this point in the run — the texture budget table is the perf lane's
deliverable (contract 3) and the working tree was mid-edit. After integration the orchestrator runs the same command;
`REQUIRE_TAG=1` turns the tag row into a gate once the tag exists. The script is read-only and exits 1 on any failing gate.

## 5. Findings

1. `/dev/mode/*`, `/dev/anim`, `/dev/rig` 404 in production (verified), so `scripts/gauntlet.sh` cannot serve as the
   production sweep; the shipping-route set (§3a/3b) is. Pass 2 reached the same conclusion and used
   `scripts/gauntlet-play.sh` — that script's 20-route set is the one to run for a full production sweep next time
   (this lane ran 10 of its routes plus the phone rows on the orchestrator's instruction, all clean).
2. The production build is 62.8 s with one pre-existing warning; the type check inside the build passes.
3. `scripts/prod-serve.sh` had :3004 hard-coded; it now takes a port. No other lane file was touched.
4. `textureBudget.json` at HEAD is the schema stub (`measuredAt: null`, no modes) — the checklist reads it as FAIL until
   the perf lane writes it.

## 6. Not done in this lane

- No tag (orchestrator's at integration); nothing pushed.
- The 21 dev-harness rows, 3 login rows and 7 mobile-tier rows were not measured against production (impossible by design,
  §3); the remaining 10 `/play/*` routes of `gauntlet-play.sh` (threepoint, karate-vs, mixedcombat, surf, snowboard,
  big-air, gymnastics, baseball, soccer, carnival) were not run in this lane.
- `docs/SHIP-READINESS.md` (pass 2's sign-off) was not updated — it is not in this lane's file set; the sign-off for this
  candidate is §3–§4 above.
- Contract changes needed: none. The `node_modules` symlink and `.env.local` copy in the worktree are untracked and were
  not committed.
