# FEL — MASTER MODE LIST

**Generated.** This document was hand-maintained until 2026-09-20 and had drifted three weeks out of date: it listed
18 modes, called eight Babylon modes "Canvas 2D mockups", and knew nothing of the kart or aero modes. It claimed to
be the single source of truth while contradicting the registry, which is the only thing that actually decides what a
player can reach.

The table below is now generated from `lib/babylon/modes/registry.ts` by `scripts/mode-list-check.ts`, which fails
if the two disagree. Everything that used to live here — the per-mode benchmarks, the tier tables, the migration
notes — is in `docs/history/MASTER_MODE_LIST-2026-08-27.md`, kept as the record of what was believed at the time.

<!-- MODES:BEGIN (generated by scripts/mode-list-check.ts — do not hand-edit) -->
**28 modes in the registry, 28 enabled.**
Generated from `lib/babylon/modes/registry.ts` — the only thing that decides what is reachable.

| key | implementation | state |
|---|---|---|
| `aeroaces` | AeroAcesMode | enabled |
| `bigair` | BigAirMode | enabled |
| `brainbrawl` | BrainBrawlMode | enabled |
| `carnival` | CourtCarnivalMode | enabled |
| `dance` | DanceMode | enabled |
| `derby` | DerbyMode | enabled |
| `duel` | DuelMode | enabled |
| `dunk` | DunkMode | enabled |
| `dunkduel` | DunkDuelMode | enabled |
| `football` | FootballRushMode | enabled |
| `freerun` | FreeRunMode | enabled |
| `golf` | GolfMode | enabled |
| `karate` | KarateEndlessMode | enabled |
| `karate_vs` | KarateVSMode | enabled |
| `mixedcombat` | MixedCombatMode | enabled |
| `onevone` | OneVOneMode | enabled |
| `penalty` | PenaltyMode | enabled |
| `showdown` | ShowdownMode | enabled |
| `skateboard` | SkateRunMode | enabled |
| `snowboard_slalom` | SnowboardSlalomMode | enabled |
| `sprint` | SprintMode | enabled |
| `surf` | SurfBreakMode | enabled |
| `tennis` | TennisMode | enabled |
| `threepoint` | ThreePointMode | enabled |
| `threevthree` | ThreeVThreeMode | enabled |
| `velocitykart` | VelocityKartMode | enabled |
| `volleyball` | VolleyballMode | enabled |
| `who_scene_it` | WhoSceneItMode | enabled |
<!-- MODES:END -->
